# 狼人殺無主持人發牌網站開發規格

## 1. 專案目標

建立一個可以讓玩家不用主持人也能進行狼人殺的網站。

網站負責：

- 建立房間
- 讓玩家透過網址或房間號碼加入
- 讓玩家輸入名稱並選擇牌面
- 自動分配角色
- 自動推進夜晚與白天流程
- 根據角色顯示不同資訊與操作
- 判斷勝利條件

網站不負責：

- 語音通話
- 視訊通話
- 聊天室
- 帳號系統
- 歷史戰績

玩家預期會使用 Discord、Line、Google Meet 或其他語音軟體一起討論。

## 2. 建議技術架構

### 前端

- React
- TypeScript
- Vite
- Socket.IO Client

前端負責：

- 顯示開房與加入房間畫面
- 顯示玩家自己的遊戲視角
- 顯示可操作的按鈕與選項
- 接收後端推送的即時狀態
- 將玩家操作送回後端

前端不應該保存完整遊戲真實狀態。

### 後端

- Node.js
- TypeScript
- Express
- Socket.IO

後端負責：

- 建立與管理房間
- 管理玩家連線
- 管理遊戲狀態
- 分配角色
- 控制遊戲流程
- 驗證玩家操作是否合法
- 只把每位玩家可以知道的資訊傳給該玩家

### 資料庫

第一版不需要資料庫。

第一版房間資料可以先存在後端記憶體中。這代表 server 重啟後房間會消失，但可以大幅降低開發複雜度。

未來需要以下功能時，再加入資料庫：

- 房間重啟後仍保留
- 斷線重連更穩定
- 儲存歷史對局
- 玩家帳號
- 勝率統計

未來資料庫建議：

- 開發期：SQLite
- 正式部署：PostgreSQL

## 3. 第一版功能範圍

第一版目標是可以完整跑完一場基本狼人殺。

### 包含功能

- 選擇遊玩人數
- 設定白天發言秒數，範圍 60 到 180 秒
- 顯示角色配置
- 顯示勝利條件
- 顯示女巫技能說明
- 建立房間
- 使用房間號碼加入房間
- 使用房間網址加入房間
- 玩家輸入名稱
- 房主可在 LOBBY 階段踢除其他玩家
- 玩家選擇一張背面牌
- 全員選完牌後自動開始
- 狼人夜晚共同選擇擊殺目標
- 預言家夜晚查驗一名玩家
- 女巫夜晚選擇救人、毒人或跳過
- 白天公布死亡結果
- 白天投票放逐
- 白天投票可棄票
- 自動判斷勝利

### 暫不包含功能

- 使用者帳號
- 遊戲開始後房主踢人
- 房主手動控制流程
- 遊戲內文字聊天
- 語音
- 複雜角色
- 歷史紀錄
- 觀戰模式
- 自訂規則編輯器

## 4. 建議角色配置

第一版先支援固定配置，之後再做自訂配置。

### 6 人局

- 狼人 2
- 預言家 1
- 女巫 1
- 平民 2

### 7 人局

- 狼人 2
- 預言家 1
- 女巫 1
- 平民 3

### 8 人局

- 狼人 2
- 預言家 1
- 女巫 1
- 平民 4

### 9 人局

- 狼人 3
- 預言家 1
- 女巫 1
- 平民 4

### 10 人局

- 狼人 3
- 預言家 1
- 女巫 1
- 平民 5

### 11 人局

- 狼人 3
- 預言家 1
- 女巫 1
- 平民 6

### 12 人局

- 狼人 4
- 預言家 1
- 女巫 1
- 平民 6

這些配置之後可以調整。第一版重點是先讓流程穩定。

## 5. 勝利條件

第一版採用簡化勝利條件：

- 狼人勝利：狼人數量大於或等於好人數量
- 好人勝利：所有狼人死亡

好人包含：

- 平民
- 預言家
- 女巫

未來可以加入更細的規則，例如屠邊、屠城、第三方陣營。

## 6. 女巫規則

第一版女巫有兩瓶藥：

- 解藥：可以救當晚被狼人擊殺的玩家
- 毒藥：可以毒死一名玩家

限制：

- 解藥只能使用一次
- 毒藥只能使用一次
- 同一晚只能選擇救人或毒人，不能同時使用解藥與毒藥
- 女巫死亡後不能再使用藥
- 女巫不能毒死亡玩家
- 女巫是否可以自救由開房設定決定

女巫自救設定：

- 只能第一晚自救
- 隨時可以自救

預設值：

- 只能第一晚自救

第一版建議規則：

- 女巫每晚只進行一次選擇
- 可選：救人、毒人、跳過
- 若解藥已用完，不顯示救人選項
- 若毒藥已用完，不顯示毒人選項

## 7. 使用者流程

### 開房流程

1. 使用者進入首頁
2. 選擇遊玩人數
3. 畫面顯示對應角色配置
4. 畫面顯示勝利條件
5. 畫面顯示女巫功能
6. 使用者點擊建立房間
7. 後端產生房間號碼
8. 前端進入房間頁
9. 房間頁顯示分享網址與房間號碼

### 加入房間流程

1. 玩家透過網址或房間號碼進入房間
2. 玩家輸入名稱
3. 玩家加入等待區
4. 等待所有玩家加入
5. 所有人數到齊後，等待房主按開始
6. 房主按開始後進入選牌階段

### 選牌流程

1. 畫面顯示與玩家人數相同數量的背面牌
2. 玩家可以先點選想要的牌
3. 其他玩家可以看到每張牌目前有多少人正在選擇
4. 多人選同一張牌時，誰先按下確認卡牌，該牌就是誰的
5. 已確認的牌會鎖定，其他玩家不能再確認該牌
6. 玩家確認選牌後，在選牌階段結束前可以取消並重新選擇
7. 玩家確認選牌後先等待其他玩家
8. 所有玩家選完後，全部玩家同時看到自己的角色
9. 所有玩家確認角色後，遊戲自動開始

### 遊戲流程

1. 夜晚開始
2. 狼人行動
3. 預言家行動
4. 女巫行動
5. 天亮公布死亡
6. 白天討論並啟動倒數
7. 白天投票
8. 公布放逐結果
9. 檢查勝利條件
10. 若未結束，進入下一晚

## 8. 遊戲狀態機

後端應該使用明確狀態機管理遊戲流程。

建議狀態：

- LOBBY：等待玩家加入
- CARD_PICKING：玩家選牌
- ROLE_REVEAL：玩家確認自己的角色
- NIGHT_START：夜晚開始
- NIGHT_WEREWOLF：狼人行動
- NIGHT_SEER：預言家行動
- NIGHT_WITCH：女巫行動
- DAY_ANNOUNCEMENT：天亮公布死亡結果
- DAY_DISCUSSION：白天討論
- DAY_VOTE：白天投票
- DAY_EXILE_RESULT：公布放逐結果
- GAME_OVER：遊戲結束

每個狀態只接受合法操作。

範例：

- LOBBY 只接受加入房間、離開房間
- CARD_PICKING 只接受選牌
- NIGHT_WEREWOLF 只接受狼人投票殺人
- NIGHT_SEER 只接受預言家查驗
- NIGHT_WITCH 只接受女巫使用技能或跳過
- DAY_VOTE 只接受存活玩家投票
- GAME_OVER 不接受遊戲操作

## 9. 每個角色的視角

### 平民

平民可看到：

- 自己的角色
- 玩家名單
- 誰存活、誰死亡
- 目前遊戲階段
- 白天投票介面

平民不可看到：

- 其他玩家角色
- 夜晚行動結果
- 狼人隊友資訊

夜晚時平民看到等待畫面。

### 狼人

狼人可看到：

- 自己的角色
- 其他狼人是誰
- 玩家存活狀態
- 夜晚擊殺投票介面
- 白天投票介面

狼人不可看到：

- 預言家查驗結果
- 女巫是否用藥
- 非狼人玩家的實際角色

### 預言家

預言家可看到：

- 自己的角色
- 玩家存活狀態
- 夜晚查驗介面
- 查驗結果
- 白天投票介面

預言家限制：

- 不能查驗死亡玩家

查驗結果第一版建議只顯示：

- 狼人
- 好人

不顯示具體角色。

### 女巫

女巫可看到：

- 自己的角色
- 玩家存活狀態
- 今晚被狼人擊殺的玩家
- 可用藥狀態
- 用藥介面
- 白天投票介面

女巫不可看到：

- 狼人身份
- 預言家查驗結果
- 其他玩家具體角色

### 死亡玩家

死亡玩家可以選擇自己的觀看模式：

- 看完整遊戲流程
- 不看遊戲流程，只看到等待或結束資訊

預設值：

- 不看遊戲流程

死亡玩家不可以：

- 夜晚行動
- 白天投票
- 影響遊戲流程

## 10. 後端資料模型草案

### Room

- roomId
- status
- maxPlayers
- config
- dayDiscussionSeconds
- witchSelfSaveRule
- players
- cards
- gameState
- createdAt
- lastActivityAt
- kickedNames

`lastActivityAt` 用於房間閒置自動清除判斷，見第 13 節。

`kickedNames` 是這個房間曾經踢除過的玩家名稱清單（不分大小寫比對）。被踢玩家的 `Player` 紀錄會整筆從 `players` 移除，因此需要另外用這份清單持久記住「這個名字被踢過」，否則名字釋出後任何人都能立刻重新用同一個名字加入。此清單只存在於該房間的生命週期內，房間被清除時一併消失，不影響其他房間。

### Player

- playerId
- socketId
- name
- role
- cardIndex
- isAlive
- isConnected
- reconnectToken
- deadViewMode
- joinedAt

`reconnectToken` 是玩家加入房間時由後端產生的隨機字串，只在該玩家加入當下透過私有訊息傳送一次，前端存在 localStorage。重連時必須附上此 token 才能恢復身份，詳見第 13 節。

### GameState

- phase
- dayNumber
- nightNumber
- werewolfVotes
- seerChecks
- witchState
- dayVotes
- pendingDeaths
- lastNightDeaths
- winner

### WitchState

- hasAntidote
- hasPoison
- usedAntidoteOn
- usedPoisonOn

### Card

- cardIndex
- isSelected
- hoveringPlayerIds
- selectedByPlayerId
- role

## 11. 即時事件設計草案

前後端使用 Socket.IO 溝通。

### Client 發送給 Server

- createRoom
- joinRoom
- confirmReconnect
- leaveRoom
- kickPlayer
- pickCard
- confirmCard
- confirmRole
- setDayDiscussionSeconds
- setWitchSelfSaveRule
- setDeadViewMode
- skipDayDiscussion
- resetRoomConfig
- werewolfVote
- seerCheck
- witchAction
- dayVote
- requestRoomState

### Server 發送給 Client

- roomCreated
- roomJoined
- roomStateUpdated
- privateStateUpdated
- cardPicked
- phaseChanged
- actionRequired
- actionResult
- dayAnnouncement
- voteResult
- gameOver
- roomClosed
- playerKicked
- error

## 12. 私有狀態與公開狀態

後端要把資料分成兩種：

### Public State

所有玩家都能看到：

- 房間號碼
- 玩家名稱
- 玩家是否在線
- 玩家是否存活
- 遊戲目前階段
- 已被選走的牌位置
- 每張未鎖定牌目前有多少人正在選擇
- 白天死亡與放逐結果

### Private State

只有特定玩家能看到：

- 自己角色
- 狼人隊友
- 預言家查驗結果
- 女巫看到的夜晚死亡目標
- 女巫剩餘藥水
- 該玩家目前能執行的操作
- 自己的 reconnectToken（僅加入當下傳送一次，之後的 privateStateUpdated 不應重複附帶）

前端不應該收到完整角色列表。

## 13. 斷線與重連

### 重連身份驗證

第一版採用 reconnect token 機制，不能只靠名稱比對身份。原因：純名稱比對時，任何知道某玩家名稱的人都可以在對方斷線瞬間搶先送出確認請求，冒充該玩家取得其私有角色資訊與操作權，這是明確的資安風險。

規則：

- 玩家加入房間成功時，後端產生一組隨機 `reconnectToken`，只在當下透過該玩家的私有訊息傳送一次
- 前端將 `reconnectToken` 存在 localStorage（依 roomId 分開儲存）
- 玩家斷線後標記為離線，並保留其 `reconnectToken`
- 玩家重新連線時，若能提供與離線玩家相符的 `reconnectToken`，直接恢復身份，不需要額外確認畫面
- 若前端沒有 `reconnectToken`（例如換裝置、清過 localStorage），才走「同名 + 手動確認恢復身份」的備援流程
- 備援流程中，多人同時嘗試恢復同一個離線身份時，後端以先收到確認 request 的玩家取得該身份
- 已被恢復的離線身份不能再被其他連線取得（無論透過 token 或備援流程）
- `reconnectToken` 只能使用一次成功恢復同一個離線 session；玩家重新連上後，伺服器可選擇沿用原 token 或核發新 token 供下次斷線使用

### 斷線暫停規則

- 若遊戲中玩家斷線，遊戲流程暫停
- 暫停時不推進夜晚階段、白天倒數或投票結算
- 玩家重新連回後，若所有存活玩家都在線，遊戲繼續

建議第一版採用：

- LOBBY 和 CARD_PICKING 階段允許重新加入
- 遊戲開始後若玩家斷線，畫面提示有人離線
- 遊戲開始後斷線會暫停流程，直到玩家回來

### 房間閒置與自動清除

第一版房間資料只存在後端記憶體，若沒有清除機制，跑得越久記憶體佔用越多。建議規則：

- 每次房間有效操作（加入、選牌、遊戲行動、投票等）都更新 `lastActivityAt`
- 房間進入 `GAME_OVER` 後，若超過 30 分鐘沒有任何操作，自動從記憶體移除
- 未進入 `GAME_OVER` 的房間（例如卡在 LOBBY 或遊戲中），若超過 2 小時沒有任何操作，也自動移除，視為廢棄房間
- 房間被自動清除前，若仍有連線中的 client，廣播一個提示事件告知房間已關閉
- 清除排程可用簡單的定時掃描（例如每分鐘檢查一次所有房間的 `lastActivityAt`），不需要引入額外排程套件

## 14. 計時器與自動推進

第一版夜晚行動使用「所有需要操作的人完成後自動前進」。

例如：

- 所有狼人都投票後，進入預言家階段
- 預言家選擇或跳過後，進入女巫階段
- 女巫選擇或跳過後，進入天亮
- 所有存活玩家投票後，公布放逐結果

白天討論階段使用倒數計時。

第一版建議：

- 夜晚行動由完成操作自動推進
- 開房時可以設定白天發言秒數，最小 60 秒，最大 180 秒
- 白天發言秒數預設為 120 秒
- 每個白天討論階段啟動倒數
- 白天討論階段可以按跳過發言
- 當所有存活玩家都按跳過發言時，直接進入投票
- 倒數結束後自動進入投票
- 若玩家斷線，倒數暫停
- 玩家重新連回後，倒數繼續

## 15. 狼人夜晚投票規則

第一版狼人夜晚規則：

- 狼人不能選擇不殺人
- 狼人可以殺狼人
- 狼人只能選擇存活玩家
- 若狼人投票最高票只有一名玩家，該玩家成為當晚擊殺目標
- 若狼人投票最高票平手，系統從平手候選人中隨機決定一名擊殺目標

## 16. 白天投票與放逐規則

第一版投票規則：

- 只有存活玩家可以投票
- 每位存活玩家一票
- 玩家可以投給任一存活玩家
- 玩家可以棄票
- 投票完成後由後端計算結果

第一天特殊規則：

- 若沒有任何玩家獲得超過半數票，跳過放逐
- 若有玩家獲得超過半數票，該玩家被放逐

第二天起規則：

- 若最高票只有一名玩家，該玩家被放逐
- 若最高票平手，進入第二輪投票
- 第二輪投票只在平手候選人之間選擇
- 第二輪投票也可以棄票
- 若第二輪仍然平手，跳過放逐

過半定義：

- 票數必須大於存活玩家數的一半
- 例如 7 名存活玩家時，至少需要 4 票才算過半
- 棄票不算入任何玩家票數，但仍視為該玩家完成投票

## 17. 房主設計

雖然遊戲不需要主持人，但仍可有「房主」負責非遊戲裁決的管理。

房主可以：

- 建立房間
- 分享房間
- 在開房時設定白天發言秒數
- 在開房時設定女巫自救規則
- 在 LOBBY 階段開始選牌
- 房間沒滿時重新設定人數
- 在 LOBBY 階段踢除其他玩家

房主不可以：

- 查看所有角色
- 修改夜晚結果
- 替玩家操作
- 干預勝負
- 踢除自己
- 在 LOBBY 以外的階段踢除玩家

第一版建議保留房主，但房主只負責開房設定與開始遊戲，不負責控制白天流程。

房間沒滿時的重設規則：

- 房主可以重新設定遊玩人數
- 重新設定人數後，角色配置同步更新
- 若已進入選牌階段，所有玩家需要重新選牌
- 若已開始遊戲，不能重新設定人數

房主斷線規則：

- 房主在 `LOBBY` 階段斷線時，自動將房主身份轉移給下一位在線玩家（依加入順序）
- 轉移後所有玩家畫面同步更新新的房主標記
- 若房間內當下沒有其他在線玩家，房主身份保留給原房主，等待其重連
- 原房主重連後不會自動拿回房主身份，除非目前房主再次斷線觸發轉移
- `LOBBY` 之後的階段不受影響，因為房主此時只保留開房設定權限，不影響遊戲流程推進

房主踢人規則：

- 房主只能在 `LOBBY` 階段踢除玩家
- 房主不能踢除自己（房主若要離開應使用 `leaveRoom`，觸發第 17 節的房主轉移規則）
- 被踢除的玩家立即從房間的玩家列表移除，其 `reconnectToken` 立即失效
- 被踢除玩家的名稱加入該房間的 `kickedNames` 清單
- 加入房間時，若名稱出現在該房間的 `kickedNames` 清單中，拒絕加入（不分大小寫比對），藉此讓被踢除的玩家無法再用原本的名稱或 `reconnectToken` 重新加入同一個房間
- 被踢除的玩家會收到 `playerKicked` 通知，前端顯示已被移出房間並導回首頁
- 其他玩家的畫面透過 `roomStateUpdated` 同步更新，移除該玩家並更新目前人數
- `LOBBY` 以外的階段收到 `kickPlayer` 一律視為非法操作並拒絕，不影響遊戲進行中的玩家
- 第一版不支援遊戲開始後踢人；若玩家在遊戲中失聯，走第 13 節的斷線暫停與重連流程，不會被移出遊戲

## 18. 安全與作弊防護

第一版至少要做到：

- 後端保存真實角色
- 前端只收到自己可看的資訊
- 後端驗證每個操作是否合法
- 死亡玩家不能投票或夜晚行動
- 非指定角色不能送出該角色操作
- 非當前階段不能送出操作
- 玩家不能選已被選走的牌
- 預言家不能查驗死亡玩家
- 女巫不能毒死亡玩家
- 狼人不能選擇不殺人
- 狼人只能選擇存活玩家
- 每個 socket event handler 必須以 `socket.id` 反查對應的房間與玩家，不可信任 payload 內附帶的 `playerId`；否則任何玩家都能偽造他人身份送出操作（投票、用藥、查驗等）
- 重連必須驗證 `reconnectToken`，不可只靠玩家名稱比對身份（見第 13 節）
- 房間內玩家名稱必須唯一，加入時若名稱重複（且對應玩家仍在線）需拒絕加入
- 加入房間時需檢查名稱是否在該房間的 `kickedNames` 清單中，若是則拒絕加入
- 後端必須重新驗證並夾限所有 client 傳入的設定數值（例如白天發言秒數需再次限制在 60 到 180 秒之間），不可只依賴前端的輸入限制
- 對 `createRoom`、`joinRoom` 等會建立新資源的事件加入基本頻率限制（例如同一 IP 或同一 socket 每秒最多幾次），避免惡意灌爆記憶體造成服務中斷
- Socket.IO server 需設定 CORS 白名單，只允許 `CLIENT_ORIGIN` 指定的來源建立連線，避免任意網站對 server 建立 WebSocket 連線
- `kickPlayer` 只能由該房間目前的房主觸發（以 `socket.id` 反查身份確認），非房主送出一律拒絕
- `kickPlayer` 只在 `LOBBY` 階段接受，其餘階段一律拒絕
- 房主不能對自己送出 `kickPlayer`

不需要做到：

- 完整防止開發者工具作弊
- 防止同一玩家開多個帳號
- 帳號身份驗證

這些可以等正式公開使用前再補。

## 19. 開發測試方式

### 手動多視窗測試

開發時可以用多個瀏覽器視窗模擬玩家：

- Chrome 一般視窗
- Chrome 無痕視窗
- Edge
- Firefox
- 多個不同 profile

### 測試控制台

中後期可以做一個開發用頁面：

- 顯示目前所有房間
- 顯示房間狀態
- 一鍵建立測試房
- 一鍵加入多個假玩家
- 同一頁顯示多個玩家視角
- 快速推進階段

測試控制台只能在開發環境啟用，不能部署到正式環境。

建議用 build-time 環境變數（例如 Vite 的 `import.meta.env.DEV`）在編譯階段直接排除測試頁面的程式碼與路由，而不是只用 runtime 判斷；避免有人透過 query string 或其他方式在正式站意外啟用測試後門。

### 自動化測試

後端遊戲引擎應該可以不依賴 Socket.IO 單獨測試。

建議測試：

- 角色分配數量正確
- 玩家不能選已選牌
- 狼人階段只有狼人能投票
- 預言家只能查驗一次
- 預言家不能查驗死亡玩家
- 女巫藥水只能使用一次
- 女巫不能同晚救人與毒人
- 女巫不能毒死亡玩家
- 狼人平手擊殺會從平手候選人隨機決定
- 死亡玩家不能投票
- 勝利條件判斷正確
- 狀態只能依合法順序轉移

## 20. 建議專案結構

建議採用 monorepo 結構，讓前端、後端與共用型別放在同一個 repo 中管理。

```text
kill_wolf_website/
  apps/
    web/
      public/
      src/
        app/
        components/
        features/
        hooks/
        lib/
        styles/
        main.tsx
        vite-env.d.ts
      index.html
      package.json
      tsconfig.json
      vite.config.ts
    server/
      src/
        config/
        game/
        rooms/
        socket/
        http/
        utils/
        index.ts
      tests/
      package.json
      tsconfig.json
  packages/
    shared/
      src/
        constants/
        events/
        rules/
        types/
        validation/
        index.ts
      package.json
      tsconfig.json
  DEVELOPMENT_SPEC.md
  package.json
  tsconfig.base.json
```

根目錄只放 workspace 設定、共用設定與文件，不直接放前端或後端業務程式。

### apps/web

前端 React 專案，負責畫面、操作與 Socket.IO Client 連線。

建議分層：

```text
apps/web/src/
  app/
    App.tsx
    router.tsx
  components/
    Button.tsx
    Card.tsx
    Modal.tsx
    PlayerList.tsx
    PhaseBanner.tsx
  features/
    home/
      HomePage.tsx
      RoomConfigPanel.tsx
    lobby/
      LobbyPage.tsx
      ShareRoomPanel.tsx
    cardPicking/
      CardPickingPage.tsx
      CardGrid.tsx
    game/
      GamePage.tsx
      PublicGamePanel.tsx
      RoleActionPanel.tsx
    result/
      GameOverPage.tsx
    dev/
      DevMultiViewPage.tsx
  hooks/
    useSocket.ts
    useRoomState.ts
    usePrivateState.ts
  lib/
    socketClient.ts
    roomUrl.ts
    phaseText.ts
  styles/
    global.css
```

前端原則：

- 頁面元件負責組畫面
- feature 元件負責單一流程的 UI
- hooks 負責讀取 Socket 狀態與送出玩家操作
- lib 放純工具函式
- 前端不保存完整角色表
- 前端只根據 public state 與 private state 顯示畫面
- 手機網頁是第一版必要支援，因此所有頁面一開始就要用 responsive layout 設計

### apps/web/features

建議以遊戲流程切 feature：

- home：首頁、開房設定、輸入房號
- lobby：等待房、分享網址、房主開始
- cardPicking：選牌、確認、取消重選
- game：夜晚、白天、投票、死亡玩家觀看模式
- result：遊戲結束與角色公開
- dev：開發用多玩家視角測試頁

### apps/web/components

放跨流程可重用 UI。

例如：

- Button
- Modal
- PlayerList
- PhaseBanner
- Countdown
- RoleBadge
- RoomCode

這一層不直接知道狼人殺完整規則，只接收 props 顯示 UI。

### apps/server

後端 Express + Socket.IO 專案，負責真實遊戲狀態、合法性驗證、流程推進與每位玩家的私有視角。

建議分層：

```text
apps/server/src/
  config/
    env.ts
  game/
    engine.ts
    phases.ts
    roleAssignment.ts
    winConditions.ts
    publicState.ts
    privateState.ts
    timers.ts
  rooms/
    roomStore.ts
    roomService.ts
    reconnectService.ts
  socket/
    socketServer.ts
    handlers/
      roomHandlers.ts
      cardHandlers.ts
      gameActionHandlers.ts
      reconnectHandlers.ts
  http/
    healthRoutes.ts
  utils/
    createRoomId.ts
    random.ts
    assertNever.ts
  index.ts
```

後端原則：

- `roomStore` 第一版使用記憶體 Map
- `roomService` 管理建房、加入、重設人數、開始選牌
- `game/engine.ts` 管理狀態機與階段轉移
- `publicState.ts` 產生所有玩家可見資料
- `privateState.ts` 依玩家角色產生私有資料
- Socket handlers 只做輸入驗證、呼叫 service、回傳結果
- 遊戲核心邏輯不直接依賴 Socket.IO，方便自動化測試

### apps/server/game

這是後端最重要的資料夾。

職責：

- 分配角色
- 控制 phase 轉移
- 驗證目前階段可接受哪些 action
- 計算狼人擊殺目標
- 計算預言家查驗結果
- 計算女巫用藥結果
- 計算白天投票結果
- 判斷勝利條件
- 產生 public/private state

遊戲引擎應該設計成純函式或接近純函式，方便測試。

### apps/server/socket

負責 Socket.IO 連線與事件轉發。

這層不應該直接寫複雜遊戲規則。每個 handler 應該做：

1. 取得 socket 對應的 room/player
2. 驗證 payload 基本格式
3. 呼叫 roomService 或 game engine
4. broadcast public state
5. 對每個玩家推送 private state

### apps/server/rooms

管理房間生命週期。

包含：

- 建立 7 碼房間號
- 加入房間
- 同名玩家恢復身份確認
- 玩家斷線標記
- 遊戲暫停與恢復
- 房主重新設定人數
- 房主開始選牌

### apps/server/tests

第一版至少要測後端 game engine，不需要一開始就測完整 UI。

建議測試檔：

```text
apps/server/tests/
  roleAssignment.test.ts
  cardPicking.test.ts
  werewolfPhase.test.ts
  seerPhase.test.ts
  witchPhase.test.ts
  dayVote.test.ts
  winConditions.test.ts
  reconnect.test.ts
```

### packages/shared

前後端共用 TypeScript 型別、常數與事件 payload。

建議分層：

```text
packages/shared/src/
  constants/
    room.ts
    roles.ts
    phases.ts
  events/
    clientEvents.ts
    serverEvents.ts
    payloads.ts
  rules/
    roomConfigs.ts
    defaultSettings.ts
  types/
    role.ts
    phase.ts
    player.ts
    room.ts
    gameState.ts
    publicState.ts
    privateState.ts
  validation/
    roomCode.ts
    playerName.ts
  index.ts
```

共用型別例如：

- Role
- PlayerPublicState
- GamePhase
- RoomConfig
- Socket event payloads

shared 原則：

- 只放前後端都需要的定義
- 不放 React 元件
- 不放 Express 或 Socket.IO server 實作
- 不放只屬於後端的完整遊戲秘密狀態，除非前端也需要該型別
- 房間配置與預設設定可以放這裡，避免前後端顯示不一致

### 為什麼採用 monorepo

這個專案的前後端會共享很多型別。

例如：

- 房間設定
- 玩家公開資料
- 玩家私有資料
- 遊戲階段
- Socket event payload
- 角色配置

如果前端與後端分開 repo，這些型別容易不同步。monorepo 可以讓 TypeScript 在開發時就抓到資料格式不一致的問題。

### 第一版 package 管理建議

建議使用 npm workspaces。

原因：

- 你目前已經有 npm
- 不需要再安裝 pnpm 或 yarn
- 對第一版足夠
- 未來需要時仍可切換

根目錄 package scripts 可以規劃成：

- `dev`：同時啟動前端與後端
- `dev:web`：只啟動前端
- `dev:server`：只啟動後端
- `test`：跑後端測試
- `typecheck`：檢查所有 TypeScript
- `build`：建置前端與後端

### 開發環境埠號

建議預設：

- 前端：`http://localhost:5173`
- 後端：`http://localhost:3000`
- Socket.IO：與後端同一個 port

### 環境變數

第一版需要的環境變數很少。

後端：

- `PORT`
- `CLIENT_ORIGIN`

前端：

- `VITE_SERVER_URL`

第一版沒有資料庫，所以暫時不需要 database URL。

## 21. 第一版畫面清單

### 首頁

- 選擇玩家人數
- 設定白天發言秒數
- 設定女巫自救規則
- 顯示角色配置
- 顯示勝利條件
- 顯示女巫說明
- 建立房間按鈕
- 輸入房間號碼加入

### 房間等待頁

- 房間號碼
- 分享網址
- 玩家列表
- 目前人數
- 等待玩家加入
- 房主重新設定人數
- 房主可踢除玩家的按鈕（僅房主可見，僅對其他玩家顯示）
- 房主開始選牌按鈕

### 選牌頁

- 背面牌列表
- 每張牌目前被多少人選擇
- 已被選走的牌狀態
- 確認卡牌按鈕
- 所有玩家選完前不顯示角色
- 所有玩家選完後顯示自己的角色
- 等待其他玩家選牌提示

### 遊戲頁

- 目前階段
- 白天發言倒數
- 玩家列表
- 存活狀態
- 自己角色
- 角色專屬操作區
- 階段結果訊息
- 死亡後觀看模式切換

### 結束頁

- 勝利陣營
- 玩家角色公開
- 重新開房
- 回首頁

## 22. 手機網頁支援

第一版需要支援手機網頁操作。

設計要求：

- 可以在手機瀏覽器完成開房、加入房間、選牌與所有遊戲操作
- 按鈕與牌面要適合觸控
- 玩家列表在小螢幕要容易閱讀
- 夜晚角色操作不能依賴 hover
- 投票介面要避免誤觸
- 分享房間網址要方便複製

第一版不需要做原生手機 App。

## 23. 已確認決策

以下項目已確認：

1. 白天發言秒數在開房時設定
2. 白天討論倒數結束後自動進入投票
3. 女巫同一晚只能救人或毒人，不能同時使用兩瓶藥
4. 預言家查驗結果只顯示陣營
5. 玩家全部選完牌後，所有人一起看到自己的角色
6. 玩家斷線時遊戲暫停
7. 房間號碼為 7 碼
8. 房間號碼只使用英文大寫與數字
9. 第一版支援 6 到 12 人
10. 第一版需要支援手機網頁操作
11. 第一天投票若無人過半，跳過放逐
12. 第二天起投票平手時進入第二輪投票，第二輪仍平手則跳過放逐
13. 白天發言秒數最小 60 秒，最大 180 秒
14. 白天發言階段可由所有存活玩家按跳過後提前結束
15. 白天投票可以棄票
16. 狼人夜晚投票平手時，系統從平手候選人中隨機決定擊殺目標
17. 狼人不能不殺人
18. 狼人可以殺狼人
19. 預言家不能查驗死亡玩家
20. 女巫不能毒死亡玩家
21. 女巫自救規則由開房設定決定
22. 死亡玩家可以選擇看完整流程或不看
23. 選牌時其他人可看到每張牌有多少人正在選擇
24. 多人選同一張牌時，先按確認卡牌的人取得該牌
25. 房間沒滿時，房主可以重新設定人數並讓大家重新選擇
26. 白天第二輪投票可以棄票
27. 白天發言秒數預設為 120 秒
28. 女巫自救規則預設為只能第一晚自救
29. 死亡玩家預設不看完整遊戲流程
30. 同名玩家恢復離線身份時需要確認
31. 多人同時恢復同一身份時，先送達確認 request 的玩家取得身份
32. 房間滿員後不自動開始選牌，需房主按開始
33. 玩家確認選牌後，在選牌階段結束前可以取消重選
34. 第一版不需要音效或震動提示
35. 重連採用 reconnect token 驗證身份，token 遺失時才走同名手動確認的備援流程
36. 房間內玩家名稱必須唯一
37. `GAME_OVER` 後 30 分鐘無操作自動清除房間；未結束但超過 2 小時無操作也自動清除
38. 房主在 LOBBY 階段斷線時，自動轉移房主身份給下一位在線玩家
39. 後端必須重新驗證並夾限所有 client 傳入的設定數值，不可只依賴前端限制
40. `createRoom`、`joinRoom` 需加入基本頻率限制
41. 部署平台與正式環境 CORS/HTTPS 細節第一版先不指定，待選定部署平台後再補
42. 房主可以在 LOBBY 階段踢除其他玩家，遊戲開始後不支援踢人
43. 被踢除的玩家 reconnectToken 立即失效，不能再重新加入同一房間
44. 房間維護一份 kickedNames 清單，記住被踢過的名稱，避免被踢玩家用同名立刻重新加入

## 24. 待討論決策

開工前必要規則目前已確認。

後續實作時若遇到細節衝突，再依照已確認決策優先處理。

### 24.1 白天討論改為輪流發言（實作後追加決策）

第一版原設計（第 14 節）是「開放式討論、全員共用一個倒數、各自可按跳過」。實際試玩後改為輪流發言制，取代原設計：

- 發言順序：依玩家固定座位順序（加入房間的順序）輪流，死亡玩家跳過。每天的起始發言人從「上一次死亡（夜晚擊殺或白天放逐，取最近一次）」的座位下一位開始；第一天若夜晚無人死亡則從座位 0 開始。
- 時間分配：每位玩家都有完整的白天發言秒數（例如設定 120 秒，代表每人輪到時都倒數 120 秒），不是把總秒數平分。
- 「跳過發言」按鈕語意改變：只有目前輪到發言的玩家能按，按下後立即換下一位；不再是「全員都跳過才提前結束」。
- 最後一位發言完（無論倒數結束或按跳過）直接進入投票階段。
- `PublicRoomState` 新增 `discussionSpeakingOrder`（當天發言順序）與 `currentSpeakerPlayerId`（目前發言者），取代原本的 `discussionSkipRequesterIds`。

### 24.2 夜晚死亡角色不提前跳過階段（實作後追加決策）

原實作在預言家或女巫已死亡時，會在進入 `NIGHT_SEER` / `NIGHT_WITCH` 的瞬間直接跳到下一階段。實際試玩後發現這會讓其他玩家從「這一晚流程明顯變短」察覺該角色已死，等於洩漏夜晚死亡資訊。改為：

- `NIGHT_SEER`、`NIGHT_WITCH` 一律照常啟動完整的夜晚行動倒數（`NIGHT_ACTION_SECONDS`），無論該角色是否還活著；角色已死時單純沒有人送出行動，倒數結束後自動照原本的逾時流程推進到下一階段。
- 對存活的該角色玩家而言行為不變（一樣可以在倒數內行動或被倒數逾時跳過）。

### 24.3 白天投票規則統一、平手先加賽發言（實作後追加決策）

第一版原設計（第 16 節）第一天投票採「過半才放逐，否則跳過」，第二天起才是「最高票放逐，平手進第二輪」。實際試玩後改為所有天數都用同一套規則，取代原設計：

- 不分第幾天，一律是「棄票不計入任何人票數；有唯一最高票者即放逐，不需要過半」。
- 若最高票平手，不直接進入第二輪投票：改成只有平手候選人各再發言一次（新的 `DAY_TIEBREAK_DISCUSSION` 階段，沿用白天討論的發言順序機制，只是名單限縮成平手候選人，且照樣有完整的發言倒數），發言完畢後才進入第二輪投票，第二輪只能投給這些平手候選人。
- 第二輪投票若仍然平手（或無人得票），跳過放逐，直接進入下一個夜晚。
- `GamePhase` 新增 `DAY_TIEBREAK_DISCUSSION`，狀態機為 `DAY_VOTE`（第一輪平手）→ `DAY_TIEBREAK_DISCUSSION` → `DAY_VOTE`（第二輪）→ `DAY_EXILE_RESULT`。

### 24.4 遊戲頁整合成單一牌桌（實作後追加決策）

原實作在遊戲頁上方另外放一排「玩家狀態列」（純資訊，顯示名字與線上/存活狀態），角色操作區（狼人殺人、預言家查驗、女巫用毒、白天投票）則各自再放一排可點選的玩家卡片，並搭配下方的「確認」「棄票」「結束我的發言」等按鈕。實際試玩後改成只保留一份牌桌：

- 全場玩家的牌統一畫成一排蓋著的卡背圖案，牌面上印著座位順序數字與該玩家名字；自己的牌永遠排在最靠近自己（該排第一張）的位置，其餘玩家依座位順序接續排列。
- 這排牌同時是唯一的操作介面：目前階段能對誰採取行動（狼人選擊殺目標、預言家選查驗對象、女巫選解藥/毒藥對象、白天投票對象、結束自己的發言），一律靠點擊對應的牌完成，取代原本牌格下方的個別按鈕；點擊後仍會跳出確認對話框再送出高風險操作（殺人、查驗、用毒）。棄票、女巫跳過用藥則以牌排最後一張「棄票／結束行動」的特殊卡呈現，一樣是點擊即送出。
- 牌排正上方置中會顯示一個數字圓標：只有白天討論／平手加賽發言階段會顯示「目前發言者的座位數字」，其他階段留白，不額外放階段圖示。
- 每次進入新的階段，牌排一律重新從自己的牌開始顯示（重置捲動位置），避免玩家在上一階段把牌排滑到別處後，下一階段找不到自己的牌。

### 24.5 自己的牌改成翻開、牌排不再橫向拖動（實作後追加決策）

24.4 原設計是「自己的牌也蓋著，只是排在最前面」。實際試玩後改為：

- 別人的牌永遠蓋著（卡背圖案 + 座位數字 + 名字），自己的牌另外獨立畫成一張較大的翻開的牌（顯示自己實際角色的圖片），放在別人那排的下方，不用猜自己是誰。
- 別人那排卡牌改成 `flex-wrap`自動換行成多排，不使用橫向拖動的捲軸；人數多時牌會自動排成兩排以上。
- 牌排會在每個階段開始時重新掛載（用 `phase` 當 React key），確保捲動位置或殘留的滑動狀態不會延續到下一個階段，也不會讓自己的牌被捲出畫面外（自己的牌已經獨立顯示，不受這個影響，但別人那排仍套用同樣重置邏輯以求一致）。

### 24.6 新增特殊角色：獵人、守衛、騎士，開房時可自選（實作後追加決策）

第一版原本只有狼人、預言家、女巫、平民。實際試玩後加入三個可選特殊角色，開房時房主可以勾選這局要不要加入：

- **獵人**：死亡時（不論是被狼人殺死、被放逐，或決鬥失敗——只要不是被女巫毒死）可以開槍帶走一名玩家；被女巫毒死時不能開槍。開槍時機用新增的 `HUNTER_SHOOT` 階段處理：天亮結算死亡或白天放逐結算完畢後，若剛死的人是獵人且死因不是中毒，遊戲會暫停在 `HUNTER_SHOOT` 讓獵人選擇開槍對象或放棄，時限與夜晚行動相同（`NIGHT_ACTION_SECONDS`），逾時視為不開槍；開槍結算後才檢查勝負，再回到原本該進的階段（白天討論或夜晚）。
- **守衛**：新增 `NIGHT_GUARD` 階段，固定在狼人行動之前。每晚選一名玩家守護，使其免於當晚被狼人擊殺；不能連續兩晚守護同一人。若守護的對象剛好也是女巫要救的人（同守同救），該玩家仍會死亡，避免守衛加女巫變成無敵組合。`NIGHT_GUARD` 一律照常跑完整的守衛行動倒數（不論這局有沒有守衛、守衛是否還活著），理由與第 24.2 節相同。
- **騎士**：白天（討論、平票加賽發言、投票階段皆可）可以隨時公開身分並選一名存活玩家決鬥：對方是狼人就直接擊殺對方，對方是好人則騎士自己以死謝罪；決鬥後不管結果如何都直接跳過當天剩下的討論與投票，直接進入夜晚。整場遊戲只能使用一次。決鬥後騎士的身分會立即公開（`revealedRoles` 在遊戲結束前也會包含這種中途公開的角色，不用等到 `GAME_OVER`）。
- 這三個角色都算好人陣營（不影響第 5 節的勝負判斷邏輯，勝負仍是狼人數量 vs 好人數量）。
- 開房設定（首頁與等待房）新增「特殊角色」勾選區，房主勾選要加入哪些角色，系統會從平民名額扣掉對應數量（每勾選一個角色扣一個平民名額），最多能勾選的數量等於該人數設定下原本的平民數量；重新設定人數時，若目前勾選的特殊角色數量超過新人數能容納的上限，多出來的會被自動移除。後端一律重新驗證與夾限這個選擇，不信任前端送來的內容。
- 角色圖片：`/roles/hunter.png`、`/roles/guide.png`（守衛用這張圖，檔名沿用舊命名）、`/roles/knight.png`。

### 24.7 預言家查驗紀錄搬到紀錄面板（實作後追加決策）

預言家的查驗紀錄原本直接列在遊戲主畫面上，人數多、夜數多時畫面會越來越長。改成：

- 主畫面不再顯示完整查驗紀錄列表，只留一句常駐提示（提醒查驗結果在右下角「📜紀錄→我的紀錄」分頁）。
- 查驗成功的當下，旁白訊息也會附註一句提醒查驗結果已經記錄在紀錄面板。
- 右下角紀錄面板新增「我的紀錄」分頁（只有預言家會看到這個分頁），列出每一夜的查驗結果；「公開紀錄」與「我的筆記」兩個原有分頁不受影響。

## 25. 建議開發順序

建議用「可以驗收的里程碑」開發，而不是一次把所有畫面與規則混在一起做。

### Phase 1：專案骨架與開發環境

目標：

- 建立 monorepo
- 建立 `apps/web`
- 建立 `apps/server`
- 建立 `packages/shared`
- 設定 TypeScript
- 設定 npm workspaces
- 設定前後端 dev script

交付物：

- 前端可以啟動並顯示基本頁面
- 後端可以啟動並回應 health check
- shared package 可以被 web 和 server import

驗收方式：

- `npm run dev:web` 可啟動前端
- `npm run dev:server` 可啟動後端
- `npm run typecheck` 通過

### Phase 2：共用型別與規則常數

目標：

- 先定義前後端共同語言
- 避免後續事件 payload 與畫面資料格式反覆改動

交付物：

- Role
- GamePhase
- RoomConfig
- PublicRoomState
- PrivatePlayerState
- PlayerPublicState
- Socket client/server event payloads
- 6 到 12 人角色配置
- 預設設定，例如白天 120 秒、女巫第一晚自救

驗收方式：

- shared package typecheck 通過
- web 與 server 都能引用 shared 型別

### Phase 3：後端房間與玩家管理

目標：

- 先把多人房間生命週期做穩
- 暫時不進入完整遊戲流程

交付物：

- 建立 7 碼房間號
- 建房設定：人數、白天秒數、女巫自救規則
- 玩家輸入名稱加入房間
- 房主身份
- 玩家列表 public state
- 房主可在未開始前重新設定人數
- 房主可在 LOBBY 階段踢除玩家
- 斷線標記
- reconnectToken 核發與驗證
- 同名恢復身份確認機制（token 遺失時的備援流程）
- 房主斷線自動轉移
- 房間閒置自動清除排程

驗收方式：

- 多個瀏覽器可以加入同一房間
- 房主重設人數後，所有玩家畫面同步
- 房主踢除一名玩家後，該玩家被移出並無法用原身份重新加入
- 玩家斷線後狀態顯示離線
- 攜帶正確 reconnectToken 重連可直接恢復身份，不需手動確認
- 無 token 時同名重連需確認，先送達 request 者取得身份
- 房主斷線後房主標記自動轉移給下一位在線玩家
- 房間閒置超過設定時間後從記憶體移除

### Phase 4：Socket.IO 狀態同步基礎

目標：

- 建立穩定的 public/private state 推送模式
- 先不做完整角色流程

交付物：

- Socket 連線初始化
- roomStateUpdated
- privateStateUpdated
- error event
- requestRoomState
- 每次操作後統一 broadcast public state
- 每位玩家收到自己的 private state

驗收方式：

- 多視窗操作時，玩家列表與房間設定即時同步
- 不同玩家可以收到不同 private state
- 前端重新整理後可 request 最新狀態

### Phase 5：前端基本流程

目標：

- 完成可以手動測多人流程的 UI
- 不追求視覺細節，先讓流程完整

交付物：

- 首頁
- 建房設定表單
- 加入房間表單
- 房間等待頁
- 分享網址與房間號碼
- 玩家列表
- 房主開始選牌
- 手機版基本可用 layout

驗收方式：

- 使用首頁可建立房間
- 使用房號或網址可加入
- 多視窗玩家列表同步
- 房主按開始後可進入選牌階段

### Phase 6：選牌與角色揭示

目標：

- 完成選牌競爭、確認、取消重選與角色分配

交付物：

- 背面牌列表
- 每張牌目前選擇人數
- 確認卡牌
- 取消重選
- 已確認牌鎖定
- 全員選完後一起揭示自己的角色
- 所有人確認角色後開始遊戲

驗收方式：

- 多人選同一張牌時，先確認者取得
- 其他玩家不能確認已鎖定牌
- 所有人選完前不能看到角色
- 所有人選完後只看到自己的角色
- 前端仍收不到完整角色表

### Phase 7：後端遊戲引擎

目標：

- 先把核心規則寫成可測的純邏輯
- UI 只接引擎結果，不直接判斷規則

交付物：

- phase 狀態機
- 狼人夜晚投票
- 狼人平手隨機擊殺
- 預言家查驗陣營
- 女巫救人、毒人、跳過
- 女巫自救規則
- 天亮死亡結算
- 白天討論倒數與跳過
- 白天投票與棄票
- 第二輪投票
- 勝利條件
- 遊戲暫停與恢復

驗收方式：

- 後端單元測試覆蓋主要規則
- 狀態只能依合法順序轉移
- 非合法角色、死亡玩家、錯誤階段的 action 會被拒絕

### Phase 8：遊戲頁整合

目標：

- 將後端遊戲引擎接到前端，讓玩家能完整跑一局

交付物：

- 遊戲主畫面
- 目前階段提示
- 玩家列表與存活狀態
- 角色專屬操作區
- 狼人擊殺介面
- 預言家查驗介面
- 女巫用藥介面
- 白天倒數
- 跳過發言
- 白天投票與第二輪投票
- 天亮與放逐結果
- 遊戲結束頁
- 死亡玩家觀看模式切換

驗收方式：

- 6 人局可以從建房跑到遊戲結束
- 狼人、預言家、女巫、平民看到不同畫面
- 手機瀏覽器可完成所有操作
- 玩家斷線時流程暫停，恢復後繼續

### Phase 9：開發測試工具

目標：

- 降低多人測試成本
- 方便重現複雜流程

交付物：

- DevMultiViewPage
- 一鍵建立測試房
- 一鍵加入假玩家
- 快速切換玩家視角
- 顯示 public state 與目前 phase

驗收方式：

- 開發者可以不用手動開 6 個瀏覽器，也能快速檢查不同角色畫面
- 測試工具只在開發環境啟用

### Phase 10：打磨、錯誤處理與部署準備

目標：

- 把第一版整理到可以給朋友實際試玩的品質

交付物：

- 輸入錯誤提示
- 房間不存在提示
- 房間已滿提示
- 名稱格式驗證
- 手機版細節調整
- loading 與 disabled 狀態
- 基本部署設定
- README

驗收方式：

- 新玩家可以不看說明完成建房與加入
- 常見錯誤不會讓畫面卡死
- `npm run build` 通過
- 可部署到測試環境

### 建議優先順序摘要

真正開工時，建議順序是：

1. 骨架
2. shared 型別
3. 後端房間
4. Socket 同步
5. 前端基本流程
6. 選牌
7. 後端遊戲引擎
8. 遊戲頁整合
9. 測試工具
10. 打磨與部署

## 26. Agent 執行規範

這份文件的目標是讓 coding agent 可以依序完成專案。Agent 每次開工時都應該先閱讀本文件，再依照 Phase 順序執行。

### 執行原則

- 不跳 Phase，除非前一個 Phase 已通過驗收
- 每個 Phase 都要留下可執行、可驗證的狀態
- 若發現規格衝突，先停止該項實作並在回報中列出衝突點
- 不為了快速完成而把遊戲真實狀態放到前端
- 不讓前端收到完整角色表
- 後端 game engine 要能獨立測試，不依賴瀏覽器
- 每完成一個 Phase 都要執行對應檢查
- 若檢查無法執行，必須說明原因與替代驗證方式

### 每次完成回報格式

Agent 完成任一 Phase 或重要功能時，回報需包含：

- 完成的 Phase 或功能
- 修改的主要檔案
- 已執行的指令
- 測試結果
- 尚未完成或刻意延後的項目
- 下一步建議

範例：

```text
完成 Phase 3：後端房間與玩家管理

主要修改：
- apps/server/src/rooms/roomStore.ts
- apps/server/src/rooms/roomService.ts
- packages/shared/src/types/room.ts

已驗證：
- npm run typecheck 通過
- npm run test 通過
- 手動開 3 個視窗加入同一房間，玩家列表同步

下一步：
- Phase 4：Socket.IO 狀態同步基礎
```

## 27. Phase Gate 檢查清單

每個 Phase 完成後，必須符合該 Phase gate，才可以進入下一階段。

### Phase 1 Gate：專案可啟動

必須看到：

- 前端首頁或 placeholder 畫面可以在瀏覽器開啟
- 後端 health check 回傳成功
- root script 可以啟動前端與後端

必須執行：

- `npm run typecheck`

通過標準：

- 沒有 TypeScript error
- 前端與後端都能啟動
- shared package 可被前後端 import

### Phase 2 Gate：型別一致

必須看到：

- 6 到 12 人配置都定義完成
- public state 與 private state 型別分開
- client events 與 server events payload 定義完成

必須執行：

- `npm run typecheck`

通過標準：

- web 和 server 使用同一份 shared 型別
- 沒有 duplicated enum 或 duplicated role string
- 房間預設值集中定義，不散落在多個地方

### Phase 3 Gate：房間管理可用

必須看到：

- 房主建立房間後看到 7 碼房間號
- 玩家可用房間號加入
- 玩家列表即時顯示名稱、在線狀態、房主標記
- 房主可在遊戲開始前重新設定人數
- 房主可在 LOBBY 階段踢除玩家，其他人畫面同步更新
- 斷線玩家顯示離線
- 同名玩家恢復身份時出現確認流程
- 房主斷線後房主標記自動轉移

必須測試：

- 建立 6 人房
- 加入至少 3 名玩家
- 房主把人數從 6 改成 8
- 房主踢除一名玩家，確認該玩家被移出且無法用原名稱或 token 重新加入
- 關閉一名玩家視窗，用正確 reconnectToken 重新加入
- 清除 localStorage 後用同名重新加入並確認恢復身份
- 關閉房主視窗，確認房主標記轉移給下一位在線玩家

通過標準：

- 所有視窗玩家列表一致
- 斷線不會讓 server crash
- 同一個離線身份只能被一個 request 取得
- 沒有正確 reconnectToken 時無法直接恢復他人身份
- 被踢除的玩家無法重新加入同一房間
- 非房主送出踢人操作會被拒絕

### Phase 4 Gate：Socket 同步穩定

必須看到：

- public state 所有人一致
- private state 每個玩家不同
- requestRoomState 可重新取得狀態
- 操作錯誤時只顯示 error，不造成房間狀態壞掉

必須測試：

- 兩個玩家同時操作房間設定或加入
- 重新整理其中一個玩家頁面
- 送出非法 roomId 或 playerId

通過標準：

- 重整後畫面可恢復
- 非法操作被拒絕
- 沒有玩家收到不屬於自己的 private state

### Phase 5 Gate：前端基本流程可走通

必須看到：

- 首頁可設定人數、白天秒數、女巫自救規則
- 首頁顯示角色配置、勝利條件、女巫說明
- 建房後進入等待頁
- 等待頁顯示房間號碼與分享網址
- 加入者輸入名稱後出現在玩家列表
- 房主按開始後進入選牌頁

必須測試：

- 桌面瀏覽器
- 手機寬度 viewport
- 房號加入
- URL 加入

通過標準：

- 使用者不用重新整理即可看到同步更新
- 手機版按鈕可點、文字不重疊
- 房間未滿時不可非房主開始

### Phase 6 Gate：選牌正確

必須看到：

- 每張牌是背面狀態
- 未確認前可切換暫選牌
- 每張牌顯示目前有幾人正在選
- 按確認後牌被鎖定
- 已確認玩家可以取消重選
- 全員選完前沒有人看到角色
- 全員選完後每個人只看到自己的角色

必須測試：

- 兩個玩家同時選同一張牌
- 先按確認者取得該牌
- 另一名玩家收到已被選走提示
- 已確認後取消重選
- 全員確認後角色揭示

通過標準：

- 任何玩家都不能看到完整角色表
- 角色數量符合房間配置
- 已鎖定牌不能被第二人取得
- 所有人角色揭示時間一致

### Phase 7 Gate：遊戲引擎規則正確

必須看到：

- 狀態機依序推進
- 每個 phase 只接受合法 action
- 狼人、預言家、女巫、白天投票規則都有單元測試

必須執行：

- `npm run test`
- `npm run typecheck`

通過標準：

- 狼人不能空刀
- 狼人可殺狼人
- 狼人平手時從平手候選人隨機
- 預言家只能查存活玩家
- 預言家只得到陣營
- 女巫同晚不能又救又毒
- 女巫不能毒死亡玩家
- 女巫自救規則符合開房設定
- 白天投票可棄票
- 第一天無人過半跳過放逐
- 第二天起平手進第二輪
- 第二輪仍平手跳過放逐
- 好人與狼人勝利條件正確

### Phase 8 Gate：完整遊戲可玩

必須看到：

- 6 人局可以完整跑完
- 平民夜晚只看到等待
- 狼人夜晚看到隊友與擊殺介面
- 預言家夜晚看到查驗介面與陣營結果
- 女巫夜晚看到死亡目標與可用藥
- 白天顯示倒數與跳過發言
- 投票後顯示放逐結果
- 遊戲結束後公開所有角色
- 死亡玩家預設不看流程，但可切換觀看模式

必須測試：

- 6 人局完整流程
- 至少一次狼人勝利
- 至少一次好人勝利
- 玩家斷線暫停與重連恢復
- 手機 viewport 完成一次玩家操作

通過標準：

- 不需要人工改 server 狀態也能跑完一局
- 各角色畫面資訊符合權限
- 斷線時倒數與流程暫停
- 重連後可以繼續操作

### Phase 9 Gate：測試工具可用

必須看到：

- 開發環境有 DevMultiViewPage
- 可快速建立測試房
- 可加入假玩家
- 可切換或同時查看多個玩家視角
- 可看到目前 phase 與 public state

通過標準：

- 測試工具只在 development 環境開啟
- production build 不顯示測試入口
- 測試工具不能破壞正式遊戲規則

### Phase 10 Gate：可試玩

必須看到：

- 常見錯誤都有明確提示
- loading、disabled、等待狀態完整
- 手機版主要流程可操作
- README 說明如何啟動與測試

必須執行：

- `npm run typecheck`
- `npm run test`
- `npm run build`

通過標準：

- build 成功
- 沒有明顯 UI 重疊
- 新玩家能完成建房、加入、選牌、遊戲操作

## 28. 手動驗收劇本

以下劇本是給 agent 或人工測試者使用。每個劇本都應該能在本機多視窗或 DevMultiViewPage 中完成。

### 劇本 A：建房與加入

步驟：

1. 開啟首頁
2. 選擇 6 人
3. 白天發言秒數使用預設 120 秒
4. 女巫自救使用預設第一晚自救
5. 建立房間
6. 複製房間網址
7. 開啟 5 個新視窗加入
8. 每個玩家輸入不同名稱

應看到：

- 房間號碼為 7 碼
- 只包含大寫英文與數字
- 角色配置顯示 2 狼、1 預言家、1 女巫、2 平民
- 6 名玩家都出現在列表
- 房主有房主標記
- 房主看到開始選牌按鈕

### 劇本 A2：房主踢人

步驟：

1. 延續劇本 A 的房間狀態
2. 房主點擊其中一名非房主玩家的踢除按鈕
3. 被踢除的玩家嘗試用原本的視窗（保留 reconnectToken）重新整理
4. 被踢除的玩家嘗試用同一個名稱從新視窗重新加入
5. 非房主玩家嘗試對其他玩家送出踢人操作（可用開發工具直接送 socket event 測試）

應看到：

- 被踢除的玩家立即從所有人的玩家列表消失，人數更新
- 被踢除的玩家畫面顯示已被移出房間，並導回首頁
- 被踢除的玩家無法用 reconnectToken 恢復原身份
- 被踢除的玩家無法用同名重新加入同一房間
- 非房主送出的踢人操作被後端拒絕，房間狀態不受影響
- 房主無法踢除自己

### 劇本 B：選牌競爭

步驟：

1. 房主按開始選牌
2. 玩家 A 和玩家 B 同時點同一張牌
3. 確認兩邊都看到該牌目前有 2 人選擇
4. 玩家 B 先按確認
5. 玩家 A 再按確認
6. 玩家 A 改選另一張牌並確認
7. 玩家 A 取消重選
8. 玩家 A 再選第三張牌並確認

應看到：

- 玩家 B 取得第一張牌
- 玩家 A 不能取得已鎖定牌
- 玩家 A 可取消自己的確認
- 所有人選完前不顯示角色
- 所有人選完後才一起顯示自己的角色

### 劇本 C：狼人夜晚

步驟：

1. 遊戲進入夜晚狼人階段
2. 狼人玩家查看畫面
3. 平民玩家查看畫面
4. 狼人投票擊殺一名存活玩家
5. 測試狼人投票平手

應看到：

- 狼人看到其他狼人
- 狼人看到擊殺投票介面
- 平民只看到夜晚等待
- 狼人不能選擇不殺人
- 狼人可以選狼人
- 平手時系統從平手候選人中選一人

### 劇本 D：預言家夜晚

步驟：

1. 遊戲進入預言家階段
2. 預言家選擇一名存活玩家查驗
3. 嘗試查驗死亡玩家

應看到：

- 預言家只看到查驗介面
- 查驗結果只顯示狼人或好人
- 不能查驗死亡玩家
- 其他玩家不會看到查驗結果

### 劇本 E：女巫夜晚

步驟：

1. 遊戲進入女巫階段
2. 女巫查看今晚被殺目標
3. 第一晚測試自救
4. 測試同晚救人後不能毒人
5. 測試不能毒死亡玩家

應看到：

- 女巫看到今晚被狼人擊殺的人
- 第一晚可依設定自救
- 解藥與毒藥各只能用一次
- 同一晚不能同時救與毒
- 死亡玩家不能被毒
- 其他玩家不知道女巫是否用藥

### 劇本 F：白天發言與投票

步驟：

1. 天亮後進入白天討論
2. 等待倒數開始
3. 所有存活玩家按跳過發言
4. 進入投票
5. 第一日製造無人過半
6. 第二日製造平手
7. 第二輪再次製造平手

應看到：

- 白天倒數從 120 秒開始
- 全員跳過後提前進入投票
- 玩家可以棄票
- 第一天無人過半時跳過放逐
- 第二天起平手會進入第二輪
- 第二輪仍平手時跳過放逐

### 劇本 G：斷線暫停

步驟：

1. 遊戲進入白天倒數或夜晚行動
2. 關閉一名存活玩家視窗
3. 觀察其他玩家畫面
4. 用同一裝置（保留 localStorage 的 reconnectToken）重新整理加入
5. 另外測試：清除 localStorage 後改用同名手動加入，確認恢復身份

應看到：

- 玩家斷線後遊戲暫停
- 倒數停止
- 其他玩家看到有人離線
- 帶正確 reconnectToken 重連可直接恢復身份，不需手動確認
- 沒有 token 時同名恢復身份需要確認
- 恢復後遊戲繼續

### 劇本 H：勝利結算

步驟：

1. 讓所有狼人死亡
2. 重開一局或用測試工具建立狀態
3. 讓狼人數量大於或等於好人數量

應看到：

- 所有狼人死亡時好人勝利
- 狼人數量大於或等於好人數量時狼人勝利
- 遊戲結束頁公開所有玩家角色
- GAME_OVER 後不再接受遊戲操作

## 29. 自動化測試規格

第一版自動化測試以後端 game engine 為主，前端先以手動測試與少量整合測試為主。

### 單元測試必測

- 6 到 12 人角色配置數量正確
- 房間號碼長度 7 碼
- 房間號碼只含大寫英文與數字
- 玩家名稱驗證
- 房主權限
- 房主未開始前可重設人數
- 遊戲開始後不可重設人數
- 房間內玩家名稱重複時拒絕加入
- 房主可在 LOBBY 踢除玩家，被踢除玩家從房間列表移除
- 非房主送出踢人操作會被拒絕
- 房主不能踢除自己
- LOBBY 以外階段送出踢人操作會被拒絕
- 被踢除的玩家 reconnectToken 立即失效，無法用來恢復身份
- 被踢除的玩家名稱會加入該房間的 kickedNames，之後用同名加入會被拒絕
- 房間被自動清除後，其 kickedNames 不會影響其他新建房間
- 正確 reconnectToken 才能恢復離線身份
- 錯誤或缺少 reconnectToken 時需走同名手動確認備援流程，不能直接恢復身份
- 已被恢復的離線身份不能被第二個 request 再次取得
- 房主在 LOBBY 斷線後房主身份自動轉移給下一位在線玩家
- 房間 `GAME_OVER` 後超過閒置時間會被自動清除
- 白天發言秒數即使收到超出 60-180 範圍的數值，後端仍會夾限在合法範圍
- 使用他人 playerId 送出操作會被拒絕（身份必須以 socket 對應的玩家為準）
- 選牌先確認者取得牌
- 已鎖定牌不能被第二人取得
- 確認選牌後可取消重選
- 全員選牌後才分別揭示 private role
- 狼人只能在 NIGHT_WEREWOLF 行動
- 狼人不能空刀
- 狼人可以殺狼人
- 狼人平手隨機只會選平手候選人
- 預言家只能在 NIGHT_SEER 行動
- 預言家不能查死亡玩家
- 預言家結果只回傳陣營
- 女巫只能在 NIGHT_WITCH 行動
- 女巫不能同晚救人與毒人
- 女巫不能毒死亡玩家
- 女巫自救符合房間設定
- 死亡玩家不能夜晚行動
- 死亡玩家不能白天投票
- 白天投票可以棄票
- 第一天無人過半跳過放逐
- 第二天起平手進第二輪
- 第二輪仍平手跳過放逐
- 好人勝利條件
- 狼人勝利條件
- 斷線時 phase 不推進

### 整合測試建議

- Socket 建房與加入
- 多玩家 public state 同步
- private state 不外洩
- requestRoomState 可恢復畫面
- 斷線重連恢復身份
- 6 人局完整 happy path

### 前端檢查建議

- 桌面寬度不重疊
- 手機寬度不重疊
- 主要按鈕有 disabled 狀態
- loading 狀態可見
- 錯誤訊息可見
- 角色專屬操作只在對應 phase 出現

## 30. Definition of Done

第一版完成必須符合以下條件：

- 可以建立 6 到 12 人房
- 可以透過網址或房間號碼加入
- 可以輸入名稱
- 可以選牌、確認、取消重選
- 全員選完後才顯示角色
- 可以完整跑夜晚狼人、預言家、女巫流程
- 可以完整跑白天討論、投票、第二輪投票
- 可以判斷狼人勝利與好人勝利
- 可以處理斷線暫停，並以 reconnectToken 恢復身份（token 遺失時走同名確認備援）
- 房主在 LOBBY 斷線時自動轉移房主身份
- 房間閒置或結束後會自動從記憶體清除
- 手機網頁可完成所有玩家操作
- 後端測試通過
- TypeScript 檢查通過
- production build 通過
- 沒有已知會阻止完整遊戲的 bug

## 31. 核心設計原則

- 遊戲真實狀態只存在後端
- 前端只顯示自己被允許看到的資料
- 遊戲流程由狀態機管理
- 每個階段只接受合法操作
- 第一版先求完整可玩，不急著做所有角色
- 優先讓多人同步穩定
- 測試環境要能方便模擬多名玩家
